import React from 'react'
import { View, Text, TextInput, Image, Button, ActivityIndicator, FlatList } from 'react-native'

export default class HomeView extends React.Component{
    render(): React.ReactNode {
        return (
            <View>
                <Text>HomeView</Text>
            </View>
        )
    }
}
